# 🌈 Termux Alphabet Colorizer

A lightweight Python tool for **Termux** that transforms standard text into a multi-colored masterpiece using ANSI escape sequences.

---

## 🚀 Quick Setup

Open Termux and run these commands to get started:

1. **Update packages and install Python:**
   ```bash
   pkg update && pkg upgrade
   pkg install python -y
